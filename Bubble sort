/*
        <-:BUBBLE SORT:->
        
        If Left number > Right number -->Wrong
        If there are n elements in array,then n-1 iterations will be done
*/
#include<iostream>
using namespace std;
int main()
{
    int a[100];
    int n;
    cin >> n;
    for(int i=0;i<n;i++){
        cin >> a[i];
    }
    for(int i=0;i<n;i++){
        for(int j=0;j<(n-1);j++){
            if(a[j]>a[j+1]){
                int temp=a[j];
                a[j]=a[j+1];
                a[j+1]=temp;
            }
        }
    }
    for(int i=0;i<n;i++){
        cout << endl << a[i];
    }
}
